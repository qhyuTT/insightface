"""Robot person-search proof of concept."""

# ``API_VERSION`` describes the HTTP contract independently from the installed
# package version.  The latter remains at the current 0.1.0 release until a
# packaging release is intentionally cut.
API_VERSION = "0.2.0"
__version__ = "0.1.0"
