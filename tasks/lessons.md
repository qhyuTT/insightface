# Lessons

- 新增边界校验逻辑时，helper 定义、调用点和异常矩阵必须作为同一个提交单元；只提交调用点会让
  合法线上路径也在运行时 `NameError`，而全量 `ruff` 的 F821 本可在合入前直接阻断。
- replacement 流程若旧会话完成回调会擦除 manager 持有的敏感对象，就必须在停止旧会话前、
  持同一把状态锁建立独立 staging copy；先 stop 再 clone 会让同目标替换继承全零 embedding。
- 模型目录名或 alias 不是 embedding 版本。生产一致性至少要绑定 recognition 权重 SHA-256、
  输入尺寸、输出维度和预处理/TTA schema，并由运行实例 readiness 而非镜像 label 单独证明。
- `join(timeout)` 返回只表示等待结束，不表示线程已经退出。只有 `is_alive()==False` 才能发布
  “停止成功”；否则必须保留资源引用和重试机会，不能提前宣告敏感状态已清理。
- 惰性模型初始化必须先在局部变量中完成 session、输入名、provider、hash 和 shape 校验，再一次性
  发布完整 runtime；提前写实例字段会让首次失败粘住半初始化对象，后续重试误判为 ready。
- 本项目必须使用 `uv` 管理 Python 版本、依赖、虚拟环境、锁文件和命令执行；文档与验证命令统一使用 `uv sync`、`uv run`，不以裸 `pip` 作为主流程。
- InsightFace 1.x 的 Auto 检测会组合 128 与 640 尺度；不能只因 640 适合小脸就固定为 640，否则可能漏掉占据画面大部分区域的近距离人脸。登记照和视频流都应保留双尺度默认值，除非有实测数据支持覆盖。
- 登记照片与实时视频帧不能共用同一个清晰度阈值：登记照可接受稳定的轻微柔焦正脸，视频证据则需要严格过滤运动模糊；应分别配置并通过真实样本回归。
- 实时视觉 PoC 不能只交付 Swagger 和事件 API；操作者需要可视化监控页看到当前输入流、检测框、匹配置信度和搜索状态，否则即使后台持续运行也缺少可观测性。
- 目标登记除了人脸照片还要保留操作者提供的显示姓名；姓名应随搜索事件传递到前端，但不要直接拼入 HTML，避免用户输入造成 XSS。
- 人脸框短边阈值必须以推理原始帧而非浏览器预览尺寸为准。机场 1080P 场景采用分层策略比简单降低阈值更稳妥：`>=80 px` 使用 `0.55 / 3 帧 / 1.5 s`，`64-79 px` 使用 `0.60 / 4 帧 / 2 s` 并抑制候选事件，`<64 px` 拒绝；输入降采样会直接改变人脸所属档位。
- “人脸在人体框上方 60%”是人脸中心与人体轨迹的空间关联规则，不是人脸检测规则。坐姿和截断场景可以在仅有一个完整人体框包含人脸中心时放宽；多人框重叠时不能猜测归属，无人体关联时只能使用短时人脸 IoU 兜底并执行更严格证据策略。
- T4 上的机场 1080P 基线是全帧人脸检测 `10 Hz`，必要时以 `4 Hz` 对最多 `8` 个高置信人体 ROI 补检。优化召回时必须同时观察 P95 延迟、丢帧、人脸尺寸、拒绝原因和关联路径，不能只看画面上是否出现人脸框。
- 把门控条件从调用方下移到被调用方时，必须同时检查该门控原本顺带保护了哪些副作用赋值。`service.py` 的 `similarity >= threshold` 入口门移进 `TrackConfirmation.process` 后，`last_similarity` 和 `last_face_seen` 的赋值留在了 `continue` 之前，导致预览显示低于阈值的相似度、shadow 轨迹的 grace 计时被无限刷新而永不发出 `lost`。对外上报的状态字段必须与真正进入判定的证据同源：低于阈值的观测可以计入证据窗口，但不能算作一次“看见”。
- `FaceMatchPolicy` 这类策略对象的布尔字段要描述行为而不是身份。曾用 `collect_all_observations`（是否收集低分观测）间接代表“这是超小脸档”，从而决定能否走 relaxed/fallback 关联，语义在 4 处调用点串味；应拆成 `requires_strict_association`、`shadow_eligible` 等各自只表达一件事的标志。
- 多目标匹配因 Top1/Top2 差值不足而整体拒绝时，拒绝原因要同时写回两个目标。只记录到 Top1 会让被“抢走”人脸的次优目标在诊断里完全没有痕迹，排查漏检时形成盲区。
- 批量搜索的“仍待确认目标集”不能兼任“身份竞争全集”。目标找到后只应停止为它累计证据，不能把它从 Top1/Top2 gallery 删除，否则同一人继续出现时会被强制归给剩余目标并形成级联误确认。
- 文档声明的安全底线必须在配置校验和数据消费处双重成立。像 `<48px` 永不确认这样的不变量不能直接信任可调参数或上游 `accepted` 标志；即使配置未经校验，质量门、实时链路和离线链路也必须拒绝。
- 诊断计数名称必须对应准确的数据阶段。`evidence_eligible` 只能表示已通过静态门控并送入确认器，真正通过去重和采样间隔写入窗口的数量要单列为 `evidence_collected`。
- 新增成对状态事件时必须同步审查所有消费者和评估器。`tiny_shadow_confirmed` 与 `tiny_shadow_lost` 要在前端成对处理，离线报告也必须保留 Shadow 身份，不能将它降格序列化为生产 `confirmed`。
- 基于"距上次执行多久"的 Hz 限流只能约束间隔下限，不能约束占空比；一旦单轮耗时超过该间隔，判据恒为真，限流自我失效。`service.py` 的 `roi_face_detection_hz=4` 在循环被压到 1 FPS 后实际每帧都跑 ROI，把画面卡顿归因到网络是错的。可选阶段必须有基于实测成本（stage p95）的预算上限和一条 `min_processed_fps` 硬地板，否则慢会自我放大。
- 帧率不是独立指标，它决定确认能否成立。`evidence_required / evidence_window_seconds` 隐含了一个最低采样率（3/1.5s ≈ 1.33Hz，小脸 4/2s = 1.5Hz）；处理帧率低于它时确认退化成偶发事件。任何降帧率的优化都必须同时报告这个比值，`effective_config.required_sampling_hz` 就是为此存在的。
- 检测和识别的成本差一个数量级，不能绑死在一个调用里。`app.get()` 是检测+ArcFace 全跑，实测 6 张脸 497ms 中约 400ms 是嵌入。ROI 补检只需要检测结果（随后还会被 `_merge_faces` 按 IoU 去重丢掉），对每个裁剪调用 `analyze()` 等于为将被丢弃的脸付全额识别成本——旧的 8 路 ROI 通道 1487ms，改为 detect-only 后 88ms。嵌入必须延后到去重、质量门、关联都筛完之后再批量做一次。
- 给 ROI 选择加入 per-track 冷却这类副作用后，必须清查是否存在"看起来是纯谓词"的包装函数。`_needs_roi_face_pass()` 只是 `bool(_tracks_needing_roi_face_pass(...))`，加了退避计数后调用它会静默消耗冷却额度。这类函数应删除而不是保留。
- 改 `FaceBackend` 协议时必须同步 `cli.py` 的离线评估链路——它直接复用了 `SearchSession._tracks_needing_roi_face_pass` / `_analyze_person_rois` 私有方法和一个 `SimpleNamespace` 上下文。漏改的后果不是报错而是更糟：校准出的阈值来自一条生产环境不存在的流水线。
- 性能改动的验收标准是"确认数不变"，不是"帧率变高"。用 `git stash` + `person-search-eval` 跑同一素材对比 `face_observations` / `evidence_collected` / `confirmed_events`，三者必须逐项相等；再用真实模型对比新旧嵌入的 `cos` 是否为 1.0。只跑注入 fake 的单测无法发现真实后端 API 用错。
- 限流/预算判据的**分母必须匹配被限流阶段的节奏，并且要在它实际被求值的那一帧上成立**。`_roi_fits_budget` 拿 `1/target_loop_hz`（100ms）的单帧余额去卡 ROI，而 ROI 只在「刚跑完全帧人脸检测」那一帧被求值，该帧必做成本 125ms 已超预算，余额结构性为负 → 可选阶段占空比恒为 0（实测 ROI 全程只跑 1 次、跳过 1498 次），远脸召回归零。这与第 18 条的「限流只有下限没有上限」是同一个错误的两个方向：一个恒真、一个恒假。可选阶段的成本要摊到它自己的间隔上——用按目标循环速率补充的信用桶，便宜帧攒、贵帧花，并对信用**和欠账**双向截断，否则一次灾难性慢帧会留下无界欠账、再次形成饿死。
- 诊断面板漏掉一个已存在于 API 响应里的字段，等价于这个字段不存在。`rejection_counts` 一直在 `SearchMetrics.snapshot()` 里返回，但 8 个诊断格子没有它；面板能说「脸有多大」，说不出「为什么被拒」，定位零匹配时必须靠人读代码。新增统计字段时必须同步 `monitor.html`，否则它只是给未来的自己留了一个盲区。
- 跳过/拒绝类计数器要按**原因**分桶，不能只按阶段。`budget_skips = {face_roi: 1498}` 说不出是撞了 `min_processed_fps` 地板还是没有预算余额——这两者的处置完全不同（前者要减负载，后者要改判据）。拆成 `face_roi_floor` / `face_roi_credit` 几乎零成本。
- 开启一个 docstring 明写 "opt-in" 的档位时，改配置而不是翻库默认值。`tiny_face_enabled` 默认 False 被多处测试依赖：`tests/conftest.py` 的 `make_face` 默认 bbox 是 60×60，正落在 48-63 桶内，翻默认会静默改变一批用 `Settings()` 的测试的语义。部署差异用 `.env` / `deploy_t4.sh` 的 `--env` 表达，测试扰动为零。
- 进度指示器必须与它所声称的判定门同源，否则它是负资产。远脸档 `collect_all_observations=True` 让**所有**样本（含远低于阈值的）都入队，deque 又被裁到 `evidence_required`，于是 `track_progress()` 返回的 `6/6` 在几秒内饱和并永久不动——它度量的是"采集了几个"，用户读到的是"差几帧就确认"。真实卡点（428 个样本仅 5 个越过 0.64，中位数结构性达不到）只存在于诊断计数里。上报进度时必须同时给出**达标数**和**判定用的统计量**（中位数/阈值），并且这些值要走与 `_is_confirmed` 相同的取值 helper，不能各自复算。
- `suppress_candidate` 类"抑制事件"开关会连带掐断依赖该事件流的所有指标。远脸档不发 CANDIDATE，于是 `metrics.best_similarity`（只在决策时写入）永远为 None，UI 的 `BEST SIMILARITY` 恒显 `—`；而 `_target_status["best_observed_similarity"]` 一直有正确值，只是前端没读。加抑制开关时必须清查哪些指标以被抑制的事件为唯一数据源，并为它们提供不依赖该事件的回退源——否则最需要诊断的档位恰好是诊断最瞎的档位（与第 22 条"字段没渲染等于不存在"同源）。
- 多候选里挑"最有希望"的那个，排序键必须是判定量而不是计数量。`max(progress, key=观测数)` 在饱和计数器下退化为任意选择，会持续上报一个满仓但全是低分的 track，掩盖另一个真正接近阈值的 track。
- 分层策略的"只允许收紧"很容易写成一个**终身单向棘轮**，而尺寸分层的前提恰恰是"按当前观测重新判定"。`confirmation.py` 的 `_is_stricter_policy` 只在候选更严时才换策略，`else` 分支把传入的宽策略直接丢弃，于是一名旅客在远处被首次看到（55px，落进 48-63 档）后，即使走到 3 米、人脸已 120px，整条轨迹仍按 `0.64 / 6 帧 / 聚合 0.68 / 仅 person_strict` 苛判——而该尺寸本应只要 `0.55 / 3 帧`。`state.policy` 只在轨迹被删除时清空，而"由远走近"正是一条连续轨迹，所以这个 bug 精确伪装成"证据够了但相似度总差一点"。升档与降档都必须清空证据窗口，否则一个窗口里会混入不同阈值下采集的样本；且**两个方向都要有测试**，此前 `grep _is_stricter tests/` 零命中。
- 一个判定由多个门串联时，只上报其中一个门等于没上报。远脸档除了中位数 `>=0.64`，还有第二道**质量加权聚合 embedding `>=0.68`**；它此前是 `_is_confirmed` 里的局部变量，算完即弃。面板可以显示"中位 0.66 ✓"却永远不确认，而看板上没有任何东西解释原因。把它抽成 `_aggregate_similarity()` 供 `track_progress()` 与 `_is_confirmed()` 共用（同源取值，进度与判定不会漂移），再一路带到 `TargetSearchView` 和 `monitor.html`。这是第 22、25 条的同一个教训第三次出现：**判定读到的每一个量，诊断面板都必须能显示。**
- 面板把两个不同观测的字段拼成一行，等于制造一个假 bug。`last_face_px` 记的是「见过的最大脸」，`last_rejection_reason` 记的是「最大的**被拒**脸」的原因，两者各自更新，于是现场读到 `49px · 人脸过小`，而硬地板是 48——看起来像尺寸门算错了，实际是 49px 那张脸被接受了（还拿到 0.70 相似度），而 `人脸过小` 属于另一张 40px 的脸。原因和它所描述的观测必须同一次写入、同一个来源；需要两个数就给两个字段（`last_rejection_face_px`）。
- 拿实测否掉自己的假设，比拿实测确认它更值钱。本轮的根因清单里有两条是推理出来的，动手前用 skimage 的真实人脸裁剪量了一遍，两条都错：(1) 「Laplacian 方差随人脸变小而下降、固定 45 对远脸更苛刻」——实测**相反**，INTER_AREA 缩小会把边缘压进更少像素，40px 裁剪的方差（3689）比 200px（821）高 4.5 倍，固定阈值对小脸其实更宽松；归一化到 112 反而会把小脸压到门槛附近（sharp 112 / blurred 46），是收紧而非放宽。(2) 「`assess_face` 的整数截断会把 49px 量成 47px」——推导下来 `int(x2)-int(x1)` 恒等于 `int(x2-x1)` 或再大 1，**永远不会更小**，只有画面边缘裁切才会变小。两条都写进了计划、都差点改代码。凡是「某个门在某种条件下更严/更松」的论断，都要先用真实图像量一遍再动手；同时现场给的 `rejection_counts` 里本来就没有 `face_blurry`，数据早就否掉了假设，是我没先读。
- 一个策略布尔量描述两件事，就会在放宽其中一件时连带放宽另一件。`requires_strict_association` 同时表达「必须落在人体框上」和「relaxed 关联不算」，于是想让坐姿远脸（`person_relaxed`，唯一包含框，完全不含糊）可用时，一并把「允许 face-only 兜底轨迹」也打开了——那是**毫无人体证据**的一条路。拆成 `requires_strict_association` + `allows_relaxed_association` 后两个问题各自独立。这是第 12 条同一个教训的第二次出现。
- 「弱证据一律降到兜底策略」在分层系统里是错的方向。`person_relaxed` 原本无条件把策略换成小脸档（`0.60 / 4 帧`），对普通档是收紧，对远脸档（`0.64 / 6 帧 / 聚合 0.68`）却是**放宽**。判据应该是「仅当候选策略确实更严时才替换」，复用已有的 `is_stricter_policy` 即可，不要新写一个比较。
- 窗口统计量只在「收集全部观测」的档位上起作用，改之前必须先确认这一点。普通/小脸档 `collect_all_observations=False`，低于阈值的样本根本不入队，窗口里全是达标样本，中位数 ≥ 阈值恒成立——中位数在那些档位是个 no-op。第一版测试就是用 70px 脸写的，怎么调数值都不可能让 median 与 top-K 给出不同结论。真正受影响的只有远脸档。
- 分层/档位这类「按当前观测重新判定」的机制，必须配边界迟滞，否则移动场景下它自己就是故障源。换档会清空证据窗口（第 31 条要求的），而机器人一动，同一张脸的短边就在 `48/64/80` 上反复穿越，每帧都换档、每次都清窗口，最终表现为「证据恒为 0」——和「一直没检测到脸」在面板上完全同形。迟滞让翻转不再发生，比放宽清窗口规则更安全。同时档位要按**轨迹**解析一次并由 session 持有（`_track_tiers`），不能按目标各算一遍，否则多目标下同一条轨迹会被不同档位判定。
- 检测输入尺寸是远脸召回的第一性约束，不是调优项。1080P 下 SCRFD 的 640 档把 1920 压到 640（0.333×），画面 49px 的脸到网络输入只剩 ~16px，正好是 stride-8 的锚点下限，`det_score` 因此结构性达不到远脸档的 0.65——面板上表现为「检测置信度不足」，很容易被误读成「阈值定得太高」。先看输入分辨率再看阈值。InsightFace 的 `scrfd.detect(input_size=[...])` 本就支持一次多尺度并跨尺度 NMS 合并，加一档大尺度是一次调用，不需要自己接前后处理。
- 上采样类的补检通道，尺度参数要写成「下限」而不是「定值」。`roi_face_detection_size=320` 对远处 90×130 的头肩裁剪是上采样（有效），对近处 500×500 的裁剪却是**降采样**，正好把这个通道存在的意义抹掉。按裁剪尺寸自适应并设上限，小裁剪行为不变。
- 相机自身运动是一条独立的召回杀手，且伪装成「识别不准」。证据以 `track_id` 为键，纯 IoU + 常速预测在机器人平移/旋转时整帧位移，IoU 跌破阈值 → 新 ID → 证据从零开始，人再怎么清晰也攒不满窗口。相位相关估全局平移只要 1-2ms。附带发现：`velocity = det.bbox - memory.bbox` 里的 `memory.bbox` 已经被预测推进过，算出来是**预测残差**而不是速度，会自我衰减到 0；必须保留上一次**观测**到的框（`observed_bbox`）作参照，并按 `missed` 间隔数归一，否则滑行几帧后重新匹配会得到几倍过大的速度。
- 只能统计确认次数的评测框架，无法回答「这个距离能不能用」。`person-search-eval` 一直只做阈值扫描 + 事件聚合，从不输出任何一条原始相似度，所以 lessons 里连续三轮要求的「用同人/异人分布定阈值」在工具上根本做不到，阈值也就一直停在跑通流程用的默认值。区间标注没有逐脸真值时，标签可以**推导**但必须写明推导方式（区间外全部为异人；区间内只取该帧最高分那张脸为同人，其余不猜），否则报告读者会把推导当真值。
- 手搓的 `SimpleNamespace` 假 metrics 会在每次新增指标时报废一次测试。`test_face_metrics_...` 因为管线多记了一个字段就挂了，而这跟被测行为毫无关系。能用真对象（`SearchMetrics()`）就别用命名空间拼一个。
- CUDA 上「多加一个检测尺度」的代价几乎全在 ONNX **输入形状切换**上，而不在那一档的推理上。T4 实测：单档 320 = 5 ms、640 = 18 ms、1280 = 53 ms，但 `[128,640]` = 108 ms、`[128,1280]` = 124 ms、`[128,640,1280]` = 181 ms——每换一次形状约 30 ms 重规划。所以正确形态是**换掉那一档**而不是叠加：`[1280]` 单档 57 ms，比现网 `[128,640]` 的 108 ms 还便宜一半，且在实测的每个人脸尺寸上检测分都不低于旧配置（21px 脸旧配置直接漏检、新配置 0.69；52px 0.75 → 0.82）。同理，我当天刚写的「ROI 尺度按裁剪逐个取整到 32 的倍数」看着更精确，实测 233 ms/轮 vs 量化成两档 136 ms/轮——精确的自适应把预算全花在了形状切换上。凡是要引入新的推理输入形状，先量形状切换成本。
- 「不改代码默认值、只改部署环境变量」不等于遵守了约束。本文件第 4 条要求「登记照和视频流都应保留双尺度默认值」，我却把 `PERSON_SEARCH_FACE_DETECTION_SIZE=1280` 写进 `deploy_t4.sh` 与 `.env.example`，理解成「代码里的 `face_detection_size=0` 没动就算守住了」——**约束管的是生效值，不是字面量**。后果是 60 服务器上人脸登记 100% 失败（`no_face`）。凡是调优某一路径的参数，先列出它的**全部消费者**：这个变量同时被搜索路径（显式传尺度，测过）和登记路径（`detection_size=None` 回落到 `prepare()`，从没测过）消费，而实测样本只有 21–407px 的远脸，根本不含登记照尺寸。
- SCRFD 会把图**放大**到输入尺寸且无上限（`_detect_candidates` 里 `det_scale` 没有 clamp），所以「大尺度只会更好」是错的：脸在网络输入上超过约 500px 就越过 stride-32 锚点上限，**整脸漏检**而不是分低。实测 1280 单档对占画面高度 ≥45% 的人像全部 MISS，Auto 则是 0.865–0.905；`[640]` 在 0.75 处也已掉到 0.721。Auto 的 128 档真正的作用是把占满画面的脸**缩回**锚点覆盖范围内。因此近距离通道（登记照）和远距离通道（搜索帧）必须各自持有尺度，不能共用一个变量。
- 「测试全绿」钉不住部署配置。`test_backend_config.py` 只断言 `Settings().face_detection_size == 0`，即代码默认值；环境变量注入的生效值无人断言，登记路径用什么尺度也无人断言，所以 163 个测试对这次故障完全沉默。回归测试要写成「在生产配置下断言行为」（`Settings(face_detection_size=1280)` 时登记仍得到 `[128,640]`），而不是「断言默认值没被人改」。
- 「检测不到远脸」和「检测分不够」是两种不同的故障，必须用真实人脸贴入目标分辨率画布实测才能分清。`[128,640]` 在 1080P 下对**检测框短边 <45px** 的脸是**完全漏检**（不是分低），对 52px 的脸则是检得到但只有 0.75。现场读数里 1960 次检测说明用户那个距离属于后者，所以 1280 档带来的是 0.75 → 0.82 的分数抬升和更稳的关键点，而不是「从无到有」。把两者混为一谈会高估改动收益，也会错误地去调阈值。
- 一个「资源已经不在了」的 404 会被下游当成可重试，于是永不可恢复的情况被重试到窗口耗尽。证据被生命周期清空（显式停止/超时/失败）与被消费者自己 `DELETE` 释放，原本共用 `evidence_released` 404；而调度端的重试判据是状态码，404 属于「稍后再试」。结果是一条注定失败的链路占满 600 秒重试窗口，操作台上表现为「同步中」一直转，而不是立刻说清「裁剪已随任务结束销毁」。**错误码要按「调用方接下来该做什么」分类，而不是按「服务端发生了什么」**：不可恢复就必须和可重试用不同的码（这里拆出 `evidence_discarded` 410）。
- 延迟发布的事件必须在发布时重算易变字段，不能沿用构造时的快照。`target_found` 在轨迹确认时构造、在终态转换时才补发，而 `_transition` 在这中间清空了证据字节，于是补发出去的事件里 `evidence_available` 仍是 `true`、`evidence_id` 已经取不到。任何「先攒着、后统一发」的 payload，凡是描述**当前可用性**的字段都要在发布前刷新——快照记录的是历史，而消费者读到的是承诺。
- 裁剪类交接要问清「裁的是哪个框」，不要假设一个 `bbox` 字段只有一种含义。`MatchDecision.bbox` 是**人体**跟踪框（确认是按 track 做的，这本身正确），`_store_evidence` 直接拿它去裁，于是调度端收到的「命中人脸裁剪」是整个人的大图，现场读起来就是「只能看到登记照」——因为那张才是真正的人脸特写。人脸框必须单独携带（`face_bbox`），并且没有人脸框时**不提供证据**，而不是退化成人体裁剪：一张看不出是谁的图，比明确的「暂无裁剪」更糟。
- 「这条轨迹为什么没确认」必须由**判定链本身**回答，不能靠事后推断。确认是多道门串联的，而
  轨迹一消失 `_states[track_id]` 就被 `del`，攒了几个样本、卡在哪道门全部丢失——于是
  「人没被采样够」和「采样够了但分不够」在面板上完全同形，两者的处置却相反（前者要减负载/
  缩窗口，后者要标定阈值）。把门集中到唯一的 `_evaluate` 并返回**第一个失败的门**，
  `_is_confirmed` 退化成 `gate is None`，`track_progress` 与轨迹验尸渲染同一个对象。这是第
  25/28/32 条同一个教训的第四次出现，这次是把它做成结构而不是再补一个字段。
- 轨迹验尸不能读删除那一刻的证据窗口。未确认轨迹恰恰是**窗口过期之后**才被删除的，此时
  deque 必然为空，直接读会把每一条失败都归因为「帧数不足」并报告 0 个样本——一个看起来合理、
  实际恒假的诊断。要在每次判定时保留「离确认最近的一次尝试」快照（按 门序数 > 达标数 >
  窗口统计量 排序），验尸报这个快照。反向验证方式：把快照换成实时求值，6 个测试立刻失败。
- `x or fallback` 不能用于可能合法取 0 的时间戳。`first_evidence_at or timestamp` 在第一个
  样本落在 `t=0.0` 时把它当成「未设置」，于是每一次确认耗时都报 0——而这个指标存在的唯一目的
  就是回答「确认要多久」。零是合法值的地方一律 `is None`。
- 场景 profile 只能花**帧**，不能花**标定**。`transit` 可以缩短窗口、缩小采样间隔、降低票数，
  因为这些都是可测的取舍；但没有同人/异人分布就动相似度阈值，等于把误报写进默认值，而且伪装成
  「针对场景优化」。写成注册表时要把这条做成测试：断言新 profile 的每个相似度阈值都不低于
  已有 profile。
- 放宽召回的机制要绑定**失败原因**，不能绑定「结果是没确认」。离场结算只救
  `insufficient_samples`（帧数不够，人走了），绝不救 `window_statistic_low`/`aggregate_low`
  （采样够了、分数不够）——后者已经被判过一次，在出口再用更低的门槛复判，就是没有标定的降阈值
  换了个名字。同理它必须走已有的 shadow 通道并**成对**发 confirmed/lost（第 17 条），
  不新造状态。
- 测试里 `assert a is None is expected` 是链式比较，等价于 `(a is None) and (None is
  expected)`，对 `expected=True` 恒假、对 `expected=None` 恒真——两种都不是想断言的东西。
  布尔等价断言要显式加括号写成 `(a is None) == expected`。
- `cp -a` 复制出来的部署目录会把 `origin` 一起复制走。60 服务器的 release 目录是逐代
  `cp -a` 的，`origin` 指向**上一个 release 目录**而不是 GitHub（cb1c82c ← 0dffc78 ←
  1d2e477 ← ecf02f8），于是 `git fetch origin main && git merge --ff-only` 报
  「Already up to date」、`git status` 干净、流程每一步都成功——构建的却是旧代码。
  这类静默失败不能靠「命令有没有报错」判断，必须核对**结果**：
  `git rev-parse --short HEAD` 等于目标 commit 才算数。同理 `current` 符号链接上一次
  忘了更新（停在 6ceb350），照 README 的 `cp -a current` 会从错误的基线分叉。
- Dockerfile 的变量替换**只**作用于 ADD/COPY/ENV/EXPOSE/FROM/LABEL/STOPSIGNAL/USER/
  VOLUME/WORKDIR，不作用于 RUN/CMD/ENTRYPOINT/HEALTHCHECK。所以 shell 形式的
  `HEALTHCHECK CMD ... ${PERSON_SEARCH_PORT:-8000} ...` 里的 `${}` 本来就原样存进镜像、
  由运行时的 sh 展开，**不需要任何转义**。写成 compose 的 `$$` 或想当然的 `\$` 都会坏：
  `\$` 的反斜杠会留在存储的命令串里，sh 在双引号内把 `\$` 读成字面 `$`，curl 拿到未展开
  的 URL 退出 3。T4 实测：plain 形式 rc=0，`\$` 形式 rc=3，容器被标 unhealthy 而 API
  本身完全正常——健康检查失败和服务失败在这里是两回事。
- 而 `CMD-SHELL` 是 compose 的健康检查写法，Dockerfile 解析器直接拒绝
  （`Unknown type "CMD-SHELL" in HEALTHCHECK`）。它连镜像都构建不出来。
- 这两处都被一条「原样钉住目标字符串」的测试放过了：
  `assert 'CMD-SHELL curl ... $${...}' in source` 对着一个**永远构建不出来的**
  Dockerfile 长期为绿。文本断言只能证明字符串没变，不能证明它可用；给这类断言配上
  反向守卫（`assert "CMD-SHELL" not in source`、`assert r"\${PERSON_SEARCH_PORT"
  not in source`），并且逐个把已知坏形式代回去确认测试真的会红。
- `cmd | tail -5 && echo ok` 的退出码取自管道**最后一环**（`tail`），不是 `cmd`。用它做
  「重试并确认成功」的检查，会在 `git fetch` 失败时照样打印 "fetch ok"，然后
  `git merge FETCH_HEAD`（FETCH_HEAD 是空的）报 "Already up to date"——整条链路看起来
  全绿而代码没更新，和上一条 `cp -a` 复制 origin 的坑是同一种静默失败。要么别加管道，
  要么 `set -o pipefail`，但真正可靠的仍是核对**结果**（`git rev-parse --short HEAD`），
  不是核对命令有没有报错。
